//Includes-------------------------------------------
#include <iostream>     //for using cout
#include <cmath>//for math
#include <algorithm>
#include <string>
#include <cstring>
#include <stdio.h>
#include <locale>


using namespace std;
//Includes-------------------------------------------

//var------------------------------------------------
int w1,w2,w3,a,b,abso;
//var------------------------------------------------
int main(){
	cin>>a>>b;
	abso=0;
	w1=a*b;
	
	w3=a+b;

	a=sqrt(a*a);
	b=sqrt(b*b);

	if(a>b){
		w2=a-b;
	}else if(b>a){
		w2=b-a;
	}else if(a==b){
		w2=0;
	}
	
	if(w1>w3){
		swap(w1, w3);
	}
	if(w1>w2){
		swap(w1, w2);
	}
	if(w2>w3){
		swap(w2, w3);
	}

	cout <<w3<<endl;
	
}